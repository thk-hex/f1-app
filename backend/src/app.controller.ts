import { Controller } from '@nestjs/common';

@Controller()
export class AppController {
  // This controller can be used for general app-level endpoints if needed
  // Currently, all specific functionality is handled by dedicated modules
}
